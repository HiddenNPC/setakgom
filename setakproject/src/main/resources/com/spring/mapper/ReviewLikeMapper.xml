<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.mapper.ReviewLikeMapper">

	<resultMap type="com.spring.setak.ReviewLikeVO" id="reviewlikeMap">		
		<result property="review_like_id" column="review_like_id"/>
		<result property="member_id" column="member_id"/>
		<result property="review_num" column="review_num"/>
	</resultMap>
	<!-- 
	<insert id="createReviewLike">
        INSERT INTO review_like (member_id, review_num) VALUES (#{member_id}, #{review_num})
    </insert>
 	-->
    <delete id="deleteReviewLike">
        DELETE FROM review_like WHERE review_num = #{review_num} AND member_id = #{member_id}
    </delete>

    <select id="updateReviewLike">
        UPDATE review  SET review_like = 
        (SELECT COUNT(*) FROM review_like WHERE member_id = #{member_id}) WHERE review_num = #{review_num}
   </select>

   <select id="getReviewLike" resultType="int">
        SELECT COUNT(review_like_id) FROM review_like WHERE review_num = #{review_num} AND member_id = #{member_id}
   </select>

</mapper>	